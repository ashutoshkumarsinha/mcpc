[project]
name = "mcpc-test-server"
version = "0.1.0"
description = "Lightweight MCP server for testing the mcpc Swift client"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.2.0",
]

[tool.uv]
package = false
